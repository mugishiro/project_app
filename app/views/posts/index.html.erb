<div class="main posts-index">
  <div class="container">
    <% @posts.each do |post| %>
      <div class="posts-index-item">
        <div class="post-left">

          <%#  <img src="<%= "/user_images/#{post.user.image_name}" %>"> %>

          <%= link_to("回答する", "/answers/#{post.id}") %>
        </div>
        <div class="post-right">
          <%= link_to(post.content, "/posts/#{post.id}") %>
        </div>
      </div>
    <% end %>
  </div>
</div>
